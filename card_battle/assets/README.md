# Assets Directory

Place any custom card textures, fonts, sounds, or visual elements here. 

In the current implementation:
- Card geometry and visual formatting are generated dynamically using Ursina's `Entity` class.
- Colors and typography leverage Ursina's standard built-in assets to keep the codebase directly executable with zero asset file configuration.
